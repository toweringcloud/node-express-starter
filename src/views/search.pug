extends templates/layout
include mixins/movie

block content
  center
    h2=pageTitle
  p.info Please input the keyword to search movie by title.
  form(method="GET")
    input(type="text" name="keyword" placeholder="Search by title" minlnegth=2 required value=keyword)
    center
      input(type="submit" value="SEARCH")
  br
  div
    each item in movies
      +movie(item)